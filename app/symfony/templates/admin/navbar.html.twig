<nav class="main-header navbar navbar-expand-md navbar-light navbar-white">
    <div class="container">
        <a href="{{ path('app_admin_dashboard') }}" class="navbar-brand">
            <img src="{{ asset('icono.png') }}" class="brand-image" style="opacity: .9" alt="logo">
            <span class="brand-text font-weight-light">{{ "general.applicationTitle"|trans }}</span>
        </a>

        <button class="navbar-toggler order-1" type="button" data-toggle="collapse"
                data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse order-3" id="navbarCollapse">
            <!-- Left navbar links -->
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('app_create_calendar') }}" class="nav-link">{{ "navbarAdmin.createCalendar"|trans }}</a>
                </li>
                <li class="nav-item dropdown">
                    <a id="dropdownSubMenu2" href="#" data-toggle="dropdown" aria-haspopup="true"
                       aria-expanded="false" class="nav-link dropdown-toggle">{{ "navbarAdmin.users"|trans }}</a>
                    <ul aria-labelledby="dropdownSubMenu2" class="dropdown-menu border-0 shadow">
                        <li><a href="{{ path('app_admin_create-user') }}" class="dropdown-item">{{ "navbarAdmin.createUser"|trans }}</a></li>
                        <li><a href="{{ path('app_admin_modify-user') }}" class="dropdown-item">{{ "navbarAdmin.modifyUser"|trans }}</a></li>
                    </ul>
                </li>
            </ul>
        </div>

        <!-- Right navbar links -->
        {{ include('navbar_options.html.twig') }}
    </div>
</nav>